# Contributing

Guidelines:

- Use `bundle exec rspec` to run tests.
- Run `bundle exec rubocop -a` to auto-correct style issues and fix offenses before committing.
- Do not commit secrets. Copy `.env.example` to `.env` locally and populate values.
- Update `docs/` when adding or changing CLI commands or output formats.
